# PassionAI - 圃場管理API
情熱カンパニー向け圃場管理システムのバックエンドAPI。
## 起動方法
```bash
cp backend/.env.example backend/.env
# .envにANTHROPIC_API_KEYを設定
docker-compose up --build
```
## API仕様
http://localhost:8000/docs (Swagger UI)
